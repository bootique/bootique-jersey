jerseyclient:
  auth:
    valid:
      type: "apiKeyParameter"
      key: "valid"
    customValid:
      type: "apiKeyParameter"
      name: "custom_api_key"
      key: "valid"
    invalid:
      type: "apiKeyParameter"
      key: "invalid"
  targets:
    valid:
      auth: "valid"
    customValid:
      auth: "customValid"
    invalid:
      auth: "invalid"
