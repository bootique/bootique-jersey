jerseyclient:
  auth:
    valid:
      type: "apiKeyHeader"
      key: "valid"
    customValid:
      type: "apiKeyHeader"
      name: "X-Custom-Api-Key"
      key: "valid"
    invalid:
      type: "apiKeyHeader"
      key: "invalid"
  targets:
    valid:
      auth: "valid"
    customValid:
      auth: "customValid"
    invalid:
      auth: "invalid"